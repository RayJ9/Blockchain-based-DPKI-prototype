# No on-chain contract

The centralized PKI baseline is deliberately implemented without a smart
contract. Its persistent certificate and revocation state is maintained by the
OpenSSL/OCSP runtime created by the experiment runner.
